'use strict';

const arrayMerge = require('../../ll_merge.js');
const ll = require('../../linked-lists/lib/link-list.js');

describe('ArrayBinarySearch', () => {

let lla = new LinkedList();
let llb = new LinkedList();


  it('315: ', () => {
    let testVal = arrayMerge.llmerge(lla, llb);
    expect( testVal ).toEqual();
  });



});
